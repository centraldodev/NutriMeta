export {
  DEFAULT_GOALS,
  EDITABLE_GOAL_ROWS,
  MEAL_PERIOD_LABELS,
  EMPTY_TOTAL,
} from '../../constants/nutrition';

export type { EditableGoalKey, NutritionGoalMode, NutritionGoalRow } from '../../constants/nutrition';
